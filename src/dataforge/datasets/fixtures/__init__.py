"""Bundled dataset fixtures.

Files in this directory are shipped with the package and loaded via
`importlib.resources.files("dataforge.datasets.fixtures")`. Keep them
small enough to ship in the wheel; full corpora should fetch lazily.
"""
